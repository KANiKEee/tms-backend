# ============================================================
#  Environment variables for docker-compose
#  Copy this file to .env and edit the values for production
# ============================================================

# Database password (shared across all PostgreSQL instances)
DB_PASSWORD=000

# JWT secret (MUST be changed for production!)
JWT_SECRET=TmsMiniSecretKeyForJwtTokenGeneration2024VeryLongSecureKey!!
