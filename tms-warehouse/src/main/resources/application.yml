server:
  port: ${SERVER_PORT:8089}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tms_warehouse_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:000}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: ${JWT_SECRET:TmsMiniSecretKeyForJwtTokenGeneration2024VeryLongSecureKey!!}

logging:
  level:
    com.tms.warehouse.config: DEBUG
    org.springframework.security: DEBUG

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
